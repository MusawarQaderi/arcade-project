# Architecture

The project is organized around a small core engine with separate layers for hardware access, rendering, persistent storage, and individual games.

The current implementation provides the initial skeleton only. The next step is to replace the temporary boot screen loop with a proper game state system and menu flow.